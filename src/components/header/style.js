import { styled } from "@/styles";
import { Container } from "../../styles/global"

export const HeaderSyle = styled("header", {
    position: "absolute",
    top: 0,
    width: "100%",
    height: 120,
    display: "flex",
    alignItems: "center",
    zIndex: 2023,
})